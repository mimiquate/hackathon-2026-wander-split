export function SiteFooter() {
  return (
    <footer className="mx-auto box-border flex w-full max-w-[var(--page-max)] flex-wrap justify-between gap-3 border-t border-border px-[var(--gutter)] py-[var(--space-8)] font-mono text-[length:var(--text-xs)] text-text-muted">
      <span>wonderSplit — viajes en grupo sin drama</span>
      <span>Ruta Terracota v1.0</span>
    </footer>
  );
}
