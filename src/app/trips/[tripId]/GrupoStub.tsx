import { Card } from "@/components/core/Card";

export function GrupoStub() {
  return (
    <Card padding="lg">
      <p className="m-0 text-center text-[length:var(--text-sm)] text-text-muted">
        Próximamente: gestión del grupo
      </p>
    </Card>
  );
}
